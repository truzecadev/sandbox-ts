const u = require('../src/utils');

test('reverse is defined', () => { expect(u.reverse).toBeDefined(); });

test('unique is defined', () => { expect(u.unique).toBeDefined(); });

test('clamp is defined', () => { expect(u.clamp).toBeDefined(); });

test('chunk is defined', () => { expect(u.chunk).toBeDefined(); });

